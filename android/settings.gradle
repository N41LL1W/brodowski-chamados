pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    // Mudança feita aqui para permitir que o Capacitor declare seus próprios repositórios
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "brodowski-chamados"

apply from: file("capacitor.settings.gradle")

include ':app'